{{!-- <h1>User Page</h1>
<body>
    <div id="nav" class="side-nav">
        <button onclick="location.href='/editUser/{{id}}'" type="button">Edit User Profile</button>
    </div>
    
    <!–– 
    <script src="/js/checkUser.js"></script>
    <div id="profile"></div>
    <script>checkUser({{id}},{{user_super}});</script> 
    -->

<br> --}}

<div class="side-nav">
    <div id="header-nav"><h1>My Secrets</h1></div>
    <div id="records-nav">
        <table class="myRecords">
            {{!-- {{#each records}} --}}
                {{!-- <tr>
                    <td id="record-select" onclick="displayRecord()">{{record.record_name}}</td>
                </tr> --}}
            <tr>
                <td id="record-select" onclick="displayRecord()">Bank of America</td>
            </tr>
            <tr>
                <td id="record-select" onclick="displayRecord()">Gmail</td>
            </tr>
            <tr>
                <td id="record-select" onclick="displayRecord()">Facebook</td>
            </tr>
            <tr>
                <td id="record-select" onclick="displayRecord()">MyOSU</td>
            </tr>
            {{!-- {{/each}} --}}
        </table>
    </div>
    <div id="add-nav"><button id="add-new-record" onclick="newRecord()" type="button">Add a Record</button></div>
</div>

<div class="record-content" id="record">
    <table id="recordTab" class="recordTable">
        {{!-- {{#each record_data}} --}}
            <tr>
                <td id="recordName"><h2>++Record Name++</h2></td>
                {{!-- <td id="recordName"><h2>{{record.record_name}}</h2></td> --}}
            </tr>
            <tr>
                <td>++Record URL++</td>
                {{!-- <td>{{record.record_URL}}</td> --}}
            </tr>
            <tr>
                <td>++Record Password++</td>
                {{!-- <td>{{record.record_password}}</td> --}}
            </tr>
            <tr>
                <td><button id="button" onclick="" type="button">Edit Record</button></td>
            </tr>
        {{!-- {{/each}} --}}
    </table>
</div>

{{!-- 
<form id="addRecordForm" method="post">
Record name:<br>
  <input type="text" name="record_name">
  <br>
Record (password):<br>
  <input type="text" name="record_data">
  <br>
Corresponding Website URL:<br>
  <input type="text" name="record_URL">
  <br>    
  <input type="submit" id="addRecord" value="Submit">
</form>  --}}

<div class="addRecord" id="add-record" style="display: none;">
    <form class="addRecordForm" method="post">
        <label for="record_name">*Account Name: </label>
        <input type="text" name="record_name" id="record_name">
        <br><br>
        <label for="record_password">*Password: </label>
        <input type="text" name="record_password" id="record_password">
        <br><br>
        <label for="record_url">*URL: </label>
        <input type="text" name="record_url" id="record_url">
        <br><br>
        <div class="center-button">
            <button onclick="addRecord()" id="add-record-button" type="button">Add to Database</button>
        </div>  
    </form>
    <div id="required-field" style="display: none;">
        <p>*Error: Missing required field.</p>
    </div>
</div>

<script language="JavaScript">
    function displayRecord() {
        document.getElementById('record').style.display="inline";
        document.getElementById('add-record').style.display="none";

        let currentRecord = document.getElementById('record-select');

        console.log(currentRecord.record_name);
    };

    function newRecord() {
        document.getElementById('record').style.display="none";
        document.getElementById('add-record').style.display="inline";

    };

    function addRecord() {
        let rname = document.getElementById('record_name').value;
        let rpassword = document.getElementById('record_password').value;
        let rurl = document.getElementById('record_url').value;

        if (rname != '' && rpassword != '' && rurl != '') {
        //Add record to database
        }
        else {
            document.getElementById('required-field').style.display="inline";
        }
    }
</script>



